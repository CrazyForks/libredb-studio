# This rule is not used by the project libredb-studio-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants read-only access to studio.libredb.org resources.
# This role is intended for users who need visibility into these resources
# without permissions to modify them. It is ideal for monitoring purposes and limited-access viewing.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: libredb-studio-operator
    app.kubernetes.io/managed-by: kustomize
  name: libredbstudio-viewer-role
rules:
- apiGroups:
  - studio.libredb.org
  resources:
  - libredbstudios
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - studio.libredb.org
  resources:
  - libredbstudios/status
  verbs:
  - get
